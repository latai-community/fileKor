feat: add search API with FTS5 support for external tools

- Add FTS5 virtual table (files_fts) for full-text search
- Add triggers for automatic FTS5 index synchronization
- Add query_by_labels() for OR logic multi-label search
- Add search_content() for FTS5 search in filename + metadata
- Add search_files() for combined label + content search with scoring
- Export new functions from __init__.py
- Add fallback to LIKE search if FTS5 unavailable
- Scoring system: label_match (0.5), filename_match (0.3), kor_content_match (0.2)
- Configurable weights parameter for custom scoring

API for external tools:
  search_files(labels=["finance", "2026"], query="provider costs")
  # Returns ranked results with score

All 100 tests passing.